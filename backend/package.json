{
  "name": "cfb-pickem-backend",
  "version": "0.1.0",
  "private": true,
  "description": "AWS SAM backend for the CFB Pick'em app",
  "type": "commonjs",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "local-api": "sam local start-api"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "dotenv": "^17.4.2"
  }
}
